[project]
name = "ini-render"
version = "0.1.0"
description = "Isometric tractography and cortical-surface renders for the INI Austin site"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "nibabel>=5.2",
    "scipy>=1.13",
    "pillow>=10.3",
]
